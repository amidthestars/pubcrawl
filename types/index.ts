export type FeaturedPaper = {
  pmid: string;
  title: string;
  journal: string;
  pub_date: string;
  did: string;
  found: string;
  matters: string;
};

export type FillerPaper = {
  pmid: string;
  title: string;
  journal: string;
  pub_date: string;
};
